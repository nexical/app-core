datasource:
  provider: postgresql

generator:
  client:
    provider: prisma-client-js
    engineType: library
    binaryTargets:
      - native
      - linux-musl
      - debian-openssl-3.0.x
      - linux-musl-openssl-3.0.x
